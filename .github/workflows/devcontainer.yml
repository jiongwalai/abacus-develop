name: Container

on:
  create:
  push:
    branches:
      - develop
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build_container_and_push:
    runs-on: self-hosted
    if: github.repository_owner == 'jiongwalai'
    strategy:
      matrix:
        dockerfile: ["gnu","intel","cuda"]
    steps:
      - name: Force Clean Workspace
        run: |
          sudo chattr -i -R ${{ github.workspace }} 2>/dev/null || true
          sudo chown -R $USER:$USER ${{ github.workspace }}
          sudo find ${{ github.workspace }} -mindepth 1 -delete
        
      - name: Checkout
        uses: actions/checkout@v6

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/jiongwalai/abacus-${{ matrix.dockerfile }}
            crpi-bu8qy80a1odt892i.cn-guangzhou.personal.cr.aliyuncs.com/laijiongwa/abacus-${{ matrix.dockerfile }}
            docker.io/jiongwalai/abacus-${{ matrix.dockerfile }}
          tags: |
            type=semver,pattern={{version}},enable=${{ github.ref_type == 'tag' }}
            type=raw,value=latest

      - name: Fix Docker Directory Permissions
        run: |
          sudo mkdir -p /home/runner/.docker
          sudo chown -R $USER:$USER /home/runner/.docker

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Aliyun ACR
        uses: docker/login-action@v4
        with:
          registry: crpi-bu8qy80a1odt892i.cn-guangzhou.personal.cr.aliyuncs.com
          username: ${{ secrets.ALICLOUD_ACR_USERNAME }}
          password: ${{ secrets.ALICLOUD_ACR_PASSWORD }}

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Container
        uses: docker/build-push-action@v7
        with:
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          file: Dockerfile.${{ matrix.dockerfile }}
          push: true
          cache-from: type=registry,ref=ghcr.io/jiongwalai/abacus-${{ matrix.dockerfile }}:latest
          cache-to: type=inline
